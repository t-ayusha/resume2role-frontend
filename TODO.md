# TODO

- [ ] Implement theme toggle button on `src/pages/ProfilePage.tsx` (both admin and user branches)
- [ ] Add theme persistence in `localStorage` and apply via `document.documentElement.dataset.theme`
- [ ] Add light-theme CSS overrides in `src/index.css`
- [ ] Update `src/layout/PageWrapper.tsx` to avoid hardcoded dark-only `text-white` and use theme-friendly styling
- [ ] Run typecheck/build to ensure no errors

